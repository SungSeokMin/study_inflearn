import {View, Text} from 'react-native';

type SettingsProps = {};

const Settings = ({}: SettingsProps) => {
  return (
    <View>
      <Text>Settings</Text>
    </View>
  );
};

export default Settings;
